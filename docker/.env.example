# Dossier MCP Docker Configuration
# Copy to .env and set your API key

# Required: API key for authenticating MCP clients
# Generate with: node -e "console.log(crypto.randomUUID())"
DOSSIER_API_KEY=

# Optional: Override the published port (default: 3100)
# DOSSIER_PORT=3100
